<%= form_for @section do |f| %>

  <%= render partial: 'application/show_errors', locals: {item: @section} %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit :class => 'btn rounded-outline-btn' %>

<% end %>